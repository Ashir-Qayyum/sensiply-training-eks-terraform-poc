## Very minimal for testing ONLY

name: Test Runner

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/test.yaml"


jobs:
  test:
    runs-on: arc-runner-set

    steps:
      - uses: actions/checkout@v4

      - name: Test
        run: |
          echo "Helloo From EKS runner!!"
          hostname